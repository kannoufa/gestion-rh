{% extends 'admin/slide.html.twig' %}
 
{% block title %}Véhicules de service{% endblock %}

{% block body %}
<div class="container">
    <center><img src="{{asset('assets/images/logo.png')}}" style="align: right; " width="370px"></center>
    <hr>
    <h1>Véhicules de service</h1>
    <a href="{{ path('admin_parametre', {repo : 'Vehicule'}) }}" class="btn-success btn-sm">
        <i class="fa fa-plus" aria-hidden="true"></i> Ajouter un véhicule
    </a>
    <table class="table table-striped text-center">
        <thead>
            <tr>
                <th><i class="fa fa-bus" aria-hidden="true"></i> Nom</th>
                <th><i class="fa fa-barcode" aria-hidden="true"></i> Matricule</th>
                <th><i class="fa fa-trash" aria-hidden="true"></i> Supprimer</th>
            </tr>
        </thead>
        {% if documents|length > 0 %}
            <tbody>
                {% for document in documents %}
                    <tr>
                        <td>{{ document.nom }}</td>
                        <td>{{ document.matricule}}</td>
                        <td>
                            <a href="{{ path('admin_delete', {repo:'Vehicule', id:document.id}) }}" class="btn-danger btn-lg">
                                <i class="fa fa-trash" aria-hidden="true"></i>
                            </a>
                        </td>
                    </tr>      
                {% endfor %}
            </tbody>
    </table>
        {% else %}
            <center><p class="badge rounded-pill bg-danger">Aucun résultat trouvé</p></center>
        {% endif %}
        <div class="pagination">
            {{knp_pagination_render(documents) }}
        </div><br>
</div>
{% endblock %}