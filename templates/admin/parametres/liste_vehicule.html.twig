{% extends 'admin/slide.html.twig' %}
 
{% block title %}Véhicules de service{% endblock %}

{% block body %}
<div class="page-header">
    <h3 class="page-title">
        <span class="page-title-icon bg-gradient-primary text-white mr-2">
            <i class="mdi mdi-bus"></i>
        </span> Véhicules de service
    </h3>
</div>

<div class="card">
    <div class="card-body">
        <a href="{{ path('admin_parametre', {repo : 'Vehicule'}) }}" class="btn-success btn-sm">
            <i class="mdi mdi-database-plus" aria-hidden="true"></i> Ajouter un véhicule
        </a>
        
        {% if documents|length > 0 %}
        <div class="table-responsive">
            <table class="table text-center">
                <thead>
                    <tr>
                        <th>Nom</th>
                        <th>Matricule</th>
                        <th>Supprimer</th>
                    </tr>
                </thead>
                <tbody>
                    {% for document in documents %}
                        <tr>
                            <td>{{ document.nom }}</td>
                            <td>{{ document.matricule}}</td>
                            <td>
                                <a href="{{ path('admin_delete', {repo:'Vehicule', id:document.id}) }}" class="btn btn-outline-danger btn-sm btn-icon-text">
                                    <i class="mdi mdi-delete-forever text-danger"></i>
                                </a>
                            </td>
                        </tr>      
                    {% endfor %}
                </tbody>
            </table>
        </div>
        {% else %}
            <center><p class="badge rounded-pill bg-danger">Aucun résultat trouvé</p></center>
        {% endif %}
        <div class="pagination mt-4">
            {{knp_pagination_render(documents) }}
        </div>
    </div>
</div>
{% endblock %}